extends layout

block content
  h1= title
  p 메인 페이지입니다 
  p testid1 / testpass1    ,  testid2 / testpass2    두 계정으로 로그인 가능합니다.
  p 회원가입, 로그아웃등의 기능은 구현되지 않았습니다 
  p 로그아웃은 JMT 쿠키를 제거하는 것으로 로그아웃이 가능합니다.

  form.form-signin(action="/login" method="POST")
    input(name="id" type="text" placeholder="아이디" required="" autofocus="" autocomplete="off")
    input(name="password" type="password" placeholder="비밀번호" required="")
    button(type="submit") 로그인